// npm package entry. React component + framework-agnostic core + types.
export { ChatWidget } from "./react";
export type { ChatWidgetProps } from "./react";
export { createChatWidget } from "./core";
export type {
  ChatWidgetOptions,
  ChatWidgetInstance,
  ChatTheme,
} from "./types";
